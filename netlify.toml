[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_ENV = "production"
  # Add any required env var names here, e.g. FIREBASE_API_KEY = "$FIREBASE_API_KEY"
